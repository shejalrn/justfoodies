[phases.setup]
nixPkgs = ['nodejs-18_x', 'npm-9_x']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = [
  'npm run db:generate',
  'cd frontend && npm ci && npm run build'
]

[start]
cmd = 'npm start'

[variables]
NODE_ENV = 'production'
PORT = '3000'